#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '

# Load my alias file
if [ -f ~/.aliases ]; then
	source ~/.aliases
fi

